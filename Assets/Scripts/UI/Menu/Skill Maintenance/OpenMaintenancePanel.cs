using TMPro;
using UnityEngine;
using UnityEngine.UI;

public class OpenMaintenancePanel : MonoBehaviour
{
    //[SerializeField] private WeaponDatabase WeaponDatabase;

    //[SerializeField] private Button Passive; 
    //[SerializeField] private Button Q; 
    //[SerializeField] private Button E; 
    //[SerializeField] private Button F; 
    //[SerializeField] private Button Space; 
    //[SerializeField] private Button L; 
    //[SerializeField] private Button R; 
    //[SerializeField] private Button SL; 
    //[SerializeField] private Button LR; 

    //public void OnEnable()
    //{
    //    var data = SaveManager.instance.LoadRequest(SaveManager.instance.CurrentSlot);
    //    var weppon = WeaponDatabase.weapons.Find((w) => w.weaponName == data.equipedWeapon);
    //    if (weppon == null) throw new System.Exception("무기 없음 오류");
    //    Init(weppon);
    //}

    //private void Init(Weapon weapon)
    //{
    //    Passive.GetComponent<TextContainer>().ChangeText(weapon.LSkill?.skillId);
    //    Q.GetComponent<TextContainer>().ChangeText(weapon.QSkill?.skillId);
    //    E.GetComponent<TextContainer>().ChangeText(weapon.ESkill?.skillId);
    //    F.GetComponent<TextContainer>().ChangeText(weapon.FSkill?.skillId);
    //    Space.GetComponent<TextContainer>().ChangeText(weapon.SpaceSkill?.skillId);
    //    L.GetComponent<TextContainer>().ChangeText(weapon.LSkill?.skillId);
    //    R.GetComponent<TextContainer>().ChangeText(weapon.RSkill?.skillId);
    //    SL.GetComponent<TextContainer>().ChangeText(weapon.SLSkill?.skillId);
    //    LR.GetComponent<TextContainer>().ChangeText(weapon.LRSkill?.skillId);
    //}
}
